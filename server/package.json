{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "dist/server.js",
  "scripts": {
    "test": "jest",
    "start": "[ ! -d dist ] && npm run build && node dist/server.js || node dist/server.js",
    "dev": "nodemon --watch '**/*.ts' --ignore 'src/**/*.spec.ts' --exec 'ts-node' server.ts",
    "prebuild": "npm run clean",
    "build": "tsc",
    "postbuild": "cp .env dist/.env && ln -s ../.schedule.js dist/.schedule.js",
    "clean": "rm -rf dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "jsdom": "^16.5.1",
    "mongoose": "5.11.15",
    "node-schedule": "^2.0.0",
    "rand-picker": "^1.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.11",
    "@types/jest": "^26.0.20",
    "@types/jest-expect-message": "^1.0.3",
    "@types/jsdom": "^16.2.7",
    "@types/node-schedule": "^1.3.1",
    "jest": "^26.6.3",
    "jest-expect-message": "^1.0.2",
    "nodemon": "^2.0.7",
    "ts-jest": "^26.5.3",
    "tslib": "^2.1.0",
    "typescript": "^4.2.3"
  }
}