{
  "ignore": ["**/*.test.ts", "**/*.spec.ts", ".git", "node_modules"],
  "watch": ["src", "server.ts"],
  "exec": "node --inspect -r ts-node/register -r tsconfig-paths/register ./server.ts",
  "ext": "ts,yaml,json,html,handlebars,env"
}
