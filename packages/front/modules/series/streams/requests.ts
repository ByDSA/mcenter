/* eslint-disable import/prefer-default-export */
import { rootBackendUrl } from "#modules/requests";

export const backendUrls = {
  crud: {
    search: `${rootBackendUrl}/api/streams/criteria`,
  },
};